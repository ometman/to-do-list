
const pushData = (index, description, completion) => {
    const taskItem = document.querySelector('#task-input');
    let index = 1;
    let description = taskItem.value;
    let completion = false;
    
  
    }
}
